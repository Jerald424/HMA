import { StyleSheet } from 'react-native';

export const cStyle = StyleSheet.create({
  rowAlign: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  rowJustify: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
